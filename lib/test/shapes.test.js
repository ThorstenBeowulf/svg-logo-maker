const { describe } = require('yargs');
const Shapes = require('../shapes.js');

describe('Shapes', () => {
  describe('Triangle', () => {
    it('should return an svg file when render is called', () => {
      expect();
    });
  });
  describe('Square', () => {
    it('should return an svg file when render is called', () => {
      expect();
    });
  });
  describe('Circle', () => {
    it('should return an svg file when render is called', () => {
      expect();
    });
  });
})